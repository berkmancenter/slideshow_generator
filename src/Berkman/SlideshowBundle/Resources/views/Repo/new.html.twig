{% extends 'BerkmanSlideshowBundle::layout.html.twig' %}

{% block body %}
<h2>Repo creation</h2>

<div class="lightWrap">
<form action="{{ path('repo_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <button type="submit">Create</button>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('repo') }}">
            Back to the list
        </a>
    </li>
</ul>
</div>
{% endblock %}

